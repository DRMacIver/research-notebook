language: python

addons:
  apt:
    packages:
    - texlive
    - texlive-latex-recommended
    - texlive-fonts-extra
    - texlive-math-extra
    - rubber

script:
  - rubber -d notes.tex

before_deploy:
  - python scripts/date-tag.py

deploy:
  provider: releases
  api_key: "GITHUB OAUTH TOKEN"
  file: "notes.pdf"
  skip_cleanup: true
